services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pointtracker
      POSTGRES_PASSWORD: pointtracker
      POSTGRES_DB: pointtracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
